{
  "name": "csf-compass",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && npx wrangler pages deploy frontend/dist --project-name csf-compass && cd worker && npm install",
    "deploy": "npm run build && npx wrangler deploy"
  }
}
